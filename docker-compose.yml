services:
  redis:
    # Single Redis 8 (search/JSON modules included natively since 8.0).
    # Volume key kept as `redis-memory-data` so Agent Memory Server data survives.
    image: redis:8
    container_name: rtfm-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-memory-data:/data
    restart: unless-stopped

  redisinsight:
    image: redis/redisinsight:latest
    container_name: rtfm-redisinsight
    ports:
      - "8001:5540"
    volumes:
      - redisinsight-data:/data
    depends_on:
      - redis
    restart: unless-stopped

  agent-memory:
    image: redislabs/agent-memory-server:latest
    container_name: rtfm-agent-memory
    ports:
      - "8002:8000"
    environment:
      REDIS_URL: redis://redis:6379
      DISABLE_AUTH: "true"
      # Step 12: memory extraction is background work -> cheapest Gemini tier
      # (~1k req/day free). If Google quota exhausts, promotion pauses until
      # reset (fail-open); escape hatch: groq/openai/gpt-oss-20b + GROQ_API_KEY.
      # EMBEDDING_MODEL must NOT change - existing memories are 3072-dim vectors.
      GENERATION_MODEL: gemini/gemini-3.5-flash-lite
      FAST_MODEL: gemini/gemini-3.5-flash-lite
      EMBEDDING_MODEL: gemini/gemini-embedding-001
      REDISVL_VECTOR_DIMENSIONS: "3072"
      GEMINI_API_KEY: ${GOOGLE_API_KEY}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY}
      LOG_LEVEL: INFO
    depends_on:
      - redis
    restart: unless-stopped

  agent-memory-worker:
    image: redislabs/agent-memory-server:latest
    container_name: rtfm-agent-memory-worker
    environment:
      REDIS_URL: redis://redis:6379
      DISABLE_AUTH: "true"
      # Same lane choice as the API server (see comment above).
      GENERATION_MODEL: gemini/gemini-3.5-flash-lite
      FAST_MODEL: gemini/gemini-3.5-flash-lite
      EMBEDDING_MODEL: gemini/gemini-embedding-001
      REDISVL_VECTOR_DIMENSIONS: "3072"
      GEMINI_API_KEY: ${GOOGLE_API_KEY}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY}
      LOG_LEVEL: INFO
    command: ["agent-memory", "task-worker", "--concurrency", "4"]
    depends_on:
      - redis
    restart: unless-stopped

volumes:
  redis-memory-data:
  redisinsight-data:
